# 后端镜像只需 Go 源码 + config + migrations。排除移动端/构建产物。
# 注意：不再排除 web/ —— 前端服务的 Dockerfile 从仓库根构建、需要 web/ 源码（后端 COPY 会带上 web/ 源码，仅源码、可忽略）。
.git
app/
bin/
*.log
data/
meili_data/
.meili-data/
config.yaml
config.local.yaml
*.exe
.DS_Store
.idea/
.vscode/
